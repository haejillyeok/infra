# Copy values into an untracked shell env file, or export them in your shell.
# Do not commit real passwords or keys.

export TF_VAR_ssh_public_key='ssh-rsa AAAA... user@example'
export TF_VAR_deploy_ssh_public_key='ssh-rsa AAAA... deploy@example'
export TF_VAR_db_username='replace-with-db-username'
export TF_VAR_db_password='replace-with-long-random-password'
